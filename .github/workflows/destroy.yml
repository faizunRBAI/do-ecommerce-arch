# GENERATED by UDAP Build Project from .udap/pipeline.yaml — do not
# edit by hand; change the spec and re-render.
name: destroy
'on':
  workflow_dispatch: {}
jobs:
  destroy:
    runs-on: ubuntu-latest
    env:
      DIGITALOCEAN_TOKEN: ${{ secrets.DO_TOKEN }}
      TF_VAR_do_token: ${{ secrets.DO_TOKEN }}
      AWS_ACCESS_KEY_ID: ${{ secrets.SPACES_ACCESS_KEY }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.SPACES_SECRET_KEY }}
      AWS_ENDPOINT_URL_S3: ${{ secrets.SPACES_ENDPOINT }}
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_region: nyc3
      TF_VAR_db_password: ${{ secrets.DB_PASSWORD }}
      TF_VAR_django_secret_key: ${{ secrets.DJANGO_SECRET_KEY }}
    steps:
    - uses: actions/checkout@v4
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
    - id: spaces_region
      name: Derive Spaces backend region
      run: 'host="${AWS_ENDPOINT_URL_S3#https://}"

        region="${host%%.*}"

        echo "region=${region}" >> "$GITHUB_OUTPUT"

        '
    - name: Terraform init
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=${{ steps.spaces_region.outputs.region\
        \ }}\" \\\n  -backend-config=\"endpoint=${{ secrets.SPACES_ENDPOINT }}\" \\\
        \n  -backend-config=\"skip_credentials_validation=true\" \\\n  -backend-config=\"\
        skip_requesting_account_id=true\" \\\n  -backend-config=\"skip_metadata_api_check=true\"\
        \ \\\n  -backend-config=\"skip_region_validation=true\" \\\n  -backend-config=\"\
        skip_s3_checksum=true\" \\\n  -backend-config=\"use_path_style=true\"\n"
      working-directory: infra
    - name: Terraform destroy
      run: terraform destroy -input=false -auto-approve
      working-directory: infra
