# GENERATED by UDAP Build Project from .udap/pipeline.yaml — do not
# edit by hand; change the spec and re-render.
name: deploy
'on':
  workflow_dispatch: {}
jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    - name: Install linters
      run: pip install flake8 isort
    - name: Run flake8
      run: flake8 api/ do_ecommerce_arch/ --max-line-length=120 --exclude=api/migrations,api/tests
    - name: Check import order (isort)
      run: isort --check-only api/ do_ecommerce_arch/ --skip api/migrations --skip
        api/tests
  test:
    runs-on: ubuntu-latest
    needs:
    - lint
    timeout-minutes: 15
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    - name: Install dependencies
      run: pip install -r requirements.txt
    - name: Run tests
      run: python -m pytest api/tests/ -v --tb=short
      env:
        DJANGO_SETTINGS_MODULE: do_ecommerce_arch.settings
        SECRET_KEY: test-secret-key-for-ci-only
        DATABASE_URL: sqlite:///test.db
  provision:
    runs-on: ubuntu-latest
    needs:
    - test
    env:
      DIGITALOCEAN_TOKEN: ${{ secrets.DO_TOKEN }}
      TF_VAR_do_token: ${{ secrets.DO_TOKEN }}
      AWS_ACCESS_KEY_ID: ${{ secrets.SPACES_ACCESS_KEY }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.SPACES_SECRET_KEY }}
      AWS_ENDPOINT_URL_S3: ${{ secrets.SPACES_ENDPOINT }}
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_region: nyc3
      TF_VAR_db_password: ${{ secrets.DB_PASSWORD }}
      TF_VAR_django_secret_key: ${{ secrets.DJANGO_SECRET_KEY }}
    timeout-minutes: 30
    steps:
    - uses: actions/checkout@v4
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
    - id: spaces_region
      name: Derive Spaces backend region
      run: 'host="${AWS_ENDPOINT_URL_S3#https://}"

        region="${host%%.*}"

        echo "region=${region}" >> "$GITHUB_OUTPUT"

        '
    - name: Terraform init
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=${{ steps.spaces_region.outputs.region\
        \ }}\" \\\n  -backend-config=\"endpoint=${{ secrets.SPACES_ENDPOINT }}\" \\\
        \n  -backend-config=\"skip_credentials_validation=true\" \\\n  -backend-config=\"\
        skip_requesting_account_id=true\" \\\n  -backend-config=\"skip_metadata_api_check=true\"\
        \ \\\n  -backend-config=\"skip_region_validation=true\" \\\n  -backend-config=\"\
        skip_s3_checksum=true\" \\\n  -backend-config=\"use_path_style=true\"\n"
      working-directory: infra
    - name: Terraform plan
      run: terraform plan -input=false -out=tfplan
      working-directory: infra
    - name: Terraform apply
      run: terraform apply -input=false tfplan
      working-directory: infra
  configure:
    runs-on: ubuntu-latest
    needs:
    - provision
    env:
      DIGITALOCEAN_TOKEN: ${{ secrets.DO_TOKEN }}
      TF_VAR_do_token: ${{ secrets.DO_TOKEN }}
      AWS_ACCESS_KEY_ID: ${{ secrets.SPACES_ACCESS_KEY }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.SPACES_SECRET_KEY }}
      AWS_ENDPOINT_URL_S3: ${{ secrets.SPACES_ENDPOINT }}
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_region: nyc3
      TF_VAR_db_password: ${{ secrets.DB_PASSWORD }}
      TF_VAR_django_secret_key: ${{ secrets.DJANGO_SECRET_KEY }}
    timeout-minutes: 25
    steps:
    - uses: actions/checkout@v4
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
    - name: Install Ansible
      run: pip install ansible-core
    - name: Install community.general collection
      run: ansible-galaxy collection install community.general
    - name: Write SSH key
      run: 'mkdir -p ~/.ssh

        printf ''%s\n'' "$SSH_KEY" > ~/.ssh/deploy_key

        chmod 600 ~/.ssh/deploy_key

        '
      env:
        SSH_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
    - id: tf_outputs
      name: Re-read Terraform outputs
      run: "host=\"${AWS_ENDPOINT_URL_S3#https://}\"\nregion=\"${host%%.*}\"\nterraform\
        \ init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{ secrets.TF_STATE_BUCKET\
        \ }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME }}/terraform.tfstate\"\
        \ \\\n  -backend-config=\"region=${region}\" \\\n  -backend-config=\"endpoint=${{\
        \ secrets.SPACES_ENDPOINT }}\" \\\n  -backend-config=\"skip_credentials_validation=true\"\
        \ \\\n  -backend-config=\"skip_requesting_account_id=true\" \\\n  -backend-config=\"\
        skip_metadata_api_check=true\" \\\n  -backend-config=\"skip_region_validation=true\"\
        \ \\\n  -backend-config=\"skip_s3_checksum=true\" \\\n  -backend-config=\"\
        use_path_style=true\"\ndroplet_ip=$(terraform output -raw droplet_ip)\ndb_host=$(terraform\
        \ output -raw db_host)\ndb_port=$(terraform output -raw db_port)\ndb_name=$(terraform\
        \ output -raw db_name)\ndb_user=$(terraform output -raw db_user)\necho \"\
        droplet_ip=${droplet_ip}\" >> \"$GITHUB_OUTPUT\"\necho \"db_host=${db_host}\"\
        \ >> \"$GITHUB_OUTPUT\"\necho \"db_port=${db_port}\" >> \"$GITHUB_OUTPUT\"\
        \necho \"db_name=${db_name}\" >> \"$GITHUB_OUTPUT\"\necho \"db_user=${db_user}\"\
        \ >> \"$GITHUB_OUTPUT\"\n"
      working-directory: infra
    - name: Scan host key
      run: 'ssh-keyscan -H "${{ steps.tf_outputs.outputs.droplet_ip }}" >> ~/.ssh/known_hosts

        '
    - name: Generate Ansible inventory
      run: "printf '[web]\\n%s ansible_user=%s ansible_ssh_private_key_file=~/.ssh/deploy_key\\\
        n' \\\n  \"${{ steps.tf_outputs.outputs.droplet_ip }}\" \\\n  \"${{ secrets.SSH_USER\
        \ }}\" \\\n  > ansible/inventory.ini\n"
    - name: Run Ansible playbook
      run: "ansible-playbook -i ansible/inventory.ini ansible/site.yml \\\n  -e db_host=\"\
        ${{ steps.tf_outputs.outputs.db_host }}\" \\\n  -e db_port=\"${{ steps.tf_outputs.outputs.db_port\
        \ }}\" \\\n  -e db_name=\"${{ steps.tf_outputs.outputs.db_name }}\" \\\n \
        \ -e db_user=\"${{ steps.tf_outputs.outputs.db_user }}\" \\\n  -e \"db_password=$EXTRA_DB_PASSWORD\"\
        \ \\\n  -e \"django_secret_key=$EXTRA_DJANGO_SECRET\" \\\n  -e project_name=\"\
        ${{ secrets.PROJECT_NAME }}\"\n"
      env:
        ANSIBLE_HOST_KEY_CHECKING: 'False'
        EXTRA_DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
        EXTRA_DJANGO_SECRET: ${{ secrets.DJANGO_SECRET_KEY }}
  verify:
    runs-on: ubuntu-latest
    needs:
    - configure
    env:
      DIGITALOCEAN_TOKEN: ${{ secrets.DO_TOKEN }}
      TF_VAR_do_token: ${{ secrets.DO_TOKEN }}
      AWS_ACCESS_KEY_ID: ${{ secrets.SPACES_ACCESS_KEY }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.SPACES_SECRET_KEY }}
      AWS_ENDPOINT_URL_S3: ${{ secrets.SPACES_ENDPOINT }}
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_region: nyc3
      TF_VAR_db_password: ${{ secrets.DB_PASSWORD }}
      TF_VAR_django_secret_key: ${{ secrets.DJANGO_SECRET_KEY }}
    timeout-minutes: 10
    steps:
    - uses: actions/checkout@v4
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
    - id: tf_ip
      name: Re-read droplet IP
      run: "host=\"${AWS_ENDPOINT_URL_S3#https://}\"\nregion=\"${host%%.*}\"\nterraform\
        \ init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{ secrets.TF_STATE_BUCKET\
        \ }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME }}/terraform.tfstate\"\
        \ \\\n  -backend-config=\"region=${region}\" \\\n  -backend-config=\"endpoint=${{\
        \ secrets.SPACES_ENDPOINT }}\" \\\n  -backend-config=\"skip_credentials_validation=true\"\
        \ \\\n  -backend-config=\"skip_requesting_account_id=true\" \\\n  -backend-config=\"\
        skip_metadata_api_check=true\" \\\n  -backend-config=\"skip_region_validation=true\"\
        \ \\\n  -backend-config=\"skip_s3_checksum=true\" \\\n  -backend-config=\"\
        use_path_style=true\"\ndroplet_ip=$(terraform output -raw droplet_ip)\necho\
        \ \"droplet_ip=${droplet_ip}\" >> \"$GITHUB_OUTPUT\"\n"
      working-directory: infra
    - name: Health check with retries
      run: "curl --fail \\\n  --retry 15 \\\n  --retry-delay 20 \\\n  --retry-all-errors\
        \ \\\n  \"http://${{ steps.tf_ip.outputs.droplet_ip }}/health/\"\n"
